{
  "scripts": {
    "dev": "live-server client --host=localhost --port=5500 --no-browser"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "live-server": "^1.2.2",
    "prettier": "^3.1.0"
  }
}
